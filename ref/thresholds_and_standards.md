# Thresholds and standards registry

The single source of truth for every numeric threshold the harness scores against. The per-task
driving examples (`driving_example_T<NN>.md`) and the combined `driving_example.md` cite this file
rather than restating values, so a threshold is defined once and audited once.

Each entry names its **provenance**, which also says how freely it may be changed:

| Tag | Meaning | Change policy |
|---|---|---|
| `[schema]` | A definition committed in `schemas/protstruct_review.yaml` (`ResidueOutlierKind`). | Change only with the schema. |
| `[MolProbity]` | Richardson-lab Top8000 percentile standard. | Tracks the upstream standard. |
| `[literature]` | A published threshold (cited in full below). | Tracks the cited paper. |
| `[catalog]` | A value stated in `ref/catalog.yaml`. | Change with the catalog. |
| `[template]` | A cross-tool **agreement tolerance** (how close two tools must land). | Safe to tune with evidence. |
| `[calibration]` | A sanity check against a known-good case or a deposition. | Safe to tune with evidence. |
| `[benchmark]` | An agreement tolerance **measured in this repo** by running both tools over a test set (audit trail under `ref/research/`, script under `scripts/bench_*.py`). | Change by re-running the benchmark, not by argument. |

`[template]` and `[calibration]` values are engineering tolerances, not scientific claims — tune them
as the harness accumulates evidence. `[schema]`, `[MolProbity]`, and `[literature]` values track
external standards — change them only when the standard does, and update the citation.
`[benchmark]` is the strongest tag available for a cross-tool tolerance: unlike `[template]`, the
magnitude was observed rather than inferred.

> **⚠ partial record.** Round 17 audited all `[benchmark]` rows for one defect: an extreme quoted
> from a set whose *other* values were never written down. §3 and §4 hold **21 tolerance rows, 20 of
> them `[benchmark]`** — §4's *absolute geometry floors* row is `[literature]` and was never measured
> here, so "every tolerance carries `[benchmark]`" was never quite true. Those 20 rows carry **21
> benchmarked tolerances**, since the map-model row holds both CC_mask and `d_FSC_model`. Of the 20
> rows: **18 are fully backed and 2 are marked `⚠ partial record` below — and both marks are RETAIN (honest disclosed limits, not pending work)** (rounds 42 and 44 re-based every §4 `d_min ≥ 2.5 Å` X-ray figure — both band widths and the geometry row's clashscore null ratio / starting ceiling — from lost sets onto the 44 fresh named entries via `scripts/analyze_xray_band_coverage.py`, fully backing the ΔRMSD and geometry rows; rounds 45–46 re-based both vs-deposited geometry-% rows onto the 42 named entries in `round45_ids.json` — favored % on named data, and the outlier % row by making the load-bearing check per-shared-residue **classification agreement** rather than the denominator-sensitive raw % (#284); rounds 47–48 re-based the H-placement flip-set row on the named set and made its check the **confident-conflict rate** rather than the raw disagreement rate inflated by one builder's own uncertainty (#287) — **the two remaining marks (L-test, EM map-model) are both RETAIN, so every *resolvable* partial record is now resolved**), the map-model row carrying
> two separate partial figures, one per quantity.
>
> A marked figure is not wrong — it is unverifiable, and it cannot be recounted, re-medianed, or used
> to set a prior. Round 16 learned why that matters: reasoning from the three `d_FSC_model`
> magnitudes then on record put the risk to a band at four times its actual value, because only the
> alarming values had been kept.
>
> **Round 18 fixed the cause, which was systemic rather than per-row.** Ten scripts took their
> entries from an uncommitted `--ids-file` or globbed a cache, so where a row was recoverable it was
> recoverable because an author happened to paste a table into an audit trail — an accident, not a
> mechanism. **Every `bench_*.py` now commits the set it ran on**, `scripts/validate.sh` fails if one
> does not, and a set short of its published denominator says so in the script via
> `SET_IS_COMPLETE = False`. Add a new `[benchmark]` row the same way: the number is only
> re-derivable if the set ships with the script.

---

## 1. Standard outlier definitions

Committed in `schemas/protstruct_review.yaml::ResidueOutlierKind`; percentile bases are the
Richardson-lab Top8000 reference set (high-resolution ≤ 2.0 Å structures).

| Outlier | Definition | Provenance |
|---|---|---|
| Ramachandran outlier | backbone φ,ψ outside the **99.95th** percentile region | `[schema]` `[MolProbity]` |
| Rotamer outlier | side-chain χ outside the **98th** percentile favored region | `[schema]` `[MolProbity]` |
| Cβ deviation | > **0.25 Å** from ideal | `[schema]` |
| Clash | steric overlap ≥ **0.4 Å** | `[schema]` |
| Bad geometry | bond / angle / planarity > **4σ** | `[schema]` |
| Cis-peptide | non-Pro cis ω | `[schema]` |

> **Reference-set caveat.** Top8000 percentiles were built from ≤ 2.0 Å structures. Applying them to
> a lower-resolution model still computes a percentile, but the outlier bar was set against tighter
> geometry than that model can deliver — note it when a model is well below 2.0 Å.

## 2. Literature thresholds

| Quantity | Threshold | Source |
|---|---|---|
| TM-score fold call | **> 0.5** ≈ same fold; **< 0.17** ≈ random pair | Xu J, Zhang Y. *Bioinformatics* 2010; 26(7):889–895. `[literature]` |
| CC½ resolution-cutoff floor | high-resolution shell is usable down to **CC½ ≈ 0.1–0.2** (significance is sample-size-dependent: CC > 0.3 significant at n > 100, CC > 0.08 at n > 1000 — prefer a per-shell significance test or paired refinement over a fixed floor) | CC½/CC\* definition: Karplus & Diederichs. *Science* 2012; 336(6084):1030–1033. Cutoff value: **Diederichs & Karplus. *Acta Cryst.* D 2013; 69(7):1215–1222** ("Better models by discarding data?"). `[literature]` |
| L-test twinning | untwinned ⟨\|L\|⟩ ≈ **0.5**; perfect twin ≈ **0.375** | Padilla JE, Yeates TO. *Acta Cryst.* D 2003; 59:1124–1130. `[literature]` |
| CAPRI class from DockQ | High **≥ 0.80**; Medium **[0.49, 0.80)**; Acceptable **[0.23, 0.49)**; Incorrect **< 0.23** | Basu S, Wallner B. *PLOS ONE* 2016; 11(8):e0161879. `[literature]` |
| pLDDT confidence cutoff | per-residue **pLDDT ≥ 70** = confident (trim below); ≥ 90 = very high | Jumper J et al. *Nature* 2021; 596:583–589. `[literature]` |
| Phaser MR solution | translation-function **TFZ > 8** = confident/definitely-solved; 5–8 ambiguous | McCoy AJ et al. *J. Appl. Cryst.* 2007; 40:658–674. `[literature]` |
| Real-space density fit (ligand/loop) | **Accuracy: RSZD** significant at **±3σ** (misplaced atoms / unexplained density); **precision: RSZO** floor **~1σ**. RSCC/RSR have *no B-factor-independent significance criterion* (radius convention alone shifts them "wildly"), so use RSCC only as a corroboration signal with a **matched limiting-radius convention**, not as a fixed ≥0.8 bar or a fixed ±0.05 agreement tolerance | RSZD/RSZO via EDSTATS / PDB-REDO `density-fitness`; RSCC only matched-radius | Tickle IJ. *Acta Cryst.* D 2012; 68(4):454–467. `[literature]` |
| Cryo-EM map resolution (FSC) | gold-standard FSC threshold **0.143** (half-maps); model-map FSC **0.5** | Rosenthal PB, Henderson R. *J. Mol. Biol.* 2003; 333(4):721–745. `[literature]` |

## 3. Cross-tool agreement tolerances

How close the agent's tool and the independent oracle must land on the **same** input. These grade
agreement, not quality.

| Metric | Tolerance | Tools | Provenance |
|---|---|---|---|
| CA RMSD | \|Δ\| ≤ **0.03 Å**, and **only when both aligners report the same aligned-residue count** — if the counts differ, even by one, the RMSDs are over different atom sets and must not be compared; report both with their counts. Benchmarked on 20 deposited pairs incl. homologs: on matched selections max \|Δ\| **0.02 Å** (13/20 pairs); on unmatched, up to **0.66 Å**. A single-residue difference (1MBN/1MBO, 153 vs 152) alone moved RMSD by 0.15 Å. Run `phenix.superpose_models` with `morph=False trim=False` — its defaults deform the moving model, and TM-align is rigid-body | PHENIX `superpose_models` vs TM-align (`-ter 0`) | `[benchmark]` (`ref/research/tolerance_benchmark_superposition.md`) |
| Aligned-residue count | ± **2 residues**, **only within one aligner class** (both structure-based, or both sequence-based; TM-align/US-align drop distant Cα pairs by design while LSQ aligners keep the full alignment) **and with matched chain handling** — TM-align stops at the first `TER` and aligns the **first chain only** unless given `-ter 0`, which on 1A2P/1BNI is the difference between ΔN = 185 and ΔN = 31. Confirmed on 20 pairs: 13 exact, 2 off by one, 1 off by two. A larger difference is a real disagreement about the alignment — inspect it, do not widen the band | same class only, matched chain handling | `[benchmark]` (`ref/research/tolerance_benchmark_superposition.md`) |
| Clashscore | \|Δ\| ≤ **1.0**, or **20 % of the mean, whichever is larger**, **with a matched H-build convention**. Benchmarked on 10 models (clashscore 1.2–13.6): matched, median \|Δ\| **0.115**, max 2.27 (17 % on the worst model). **Mismatched** (nuclear vs electron-cloud H), median **9.95**, max **22.97** — 10–23× the tolerance, so that comparison is **void, not failed** | PHENIX/cctbx vs MolProbity standalone (`reduce` + `probe`) | `[benchmark]` (`ref/research/tolerance_benchmark_clashscore_h.md`) |
| Ramachandran / rotamer favored % | **Ramachandran: ± 0.2 pp** — measured against the wwPDB validation report over 17 entries (median **0.00 pp**, p90 0.02, max **0.16 pp**), so ± 1.0 pp was ~6× too loose. Unlike the *outlier* row this evidence is informative on all 17 entries, since favored % is never degenerate. **Rotamer favored %: ± 1.0 pp retained, not directly measurable** — the report's `rota=` attribute is the rotamer *name* (`m-10`, `mp`, …), not a favored/allowed verdict, so no reference figure exists. The underlying **assignment** is reproduced exactly, however: **8054 of 8054 residues** across 17 entries get the identical rotamer name from `phenix.rotalyze` and the wwPDB report. The **chi geometry is verified exact** — chi1 computed independently with gemmi matches `phenix.rotalyze` to ≤ **0.05°** (its printed precision) over **8054 residues**, so chi computation contributes nothing to any disagreement. The **library** is corroborated too, by a genuinely independent one: the CCP4 monomer library's own chi torsion targets, scored via `gemmi rmsz`, rank sidechains the same way — median torsion |Z| **1.50 / 3.30 / 4.33** for MolProbity Favored / Allowed / OUTLIER over 8054 sidechains. The two answer different questions (density percentile vs deviation from a target torsion) so they cannot be differenced, but the band no longer rests on an untested shared-library assumption. The favored/allowed boundary itself is **bounded rather than verified**: **3.9 %** of residues sit within ×1.25 of the 2 % Favored/Allowed cutoff, so a systematic ±25 % scoring difference could move favored % by up to 3.9 pp — above the band. ± 1.0 pp therefore **assumes the two pipelines share a rotamer library**, which the identical assignment supports but does not prove | `phenix.ramalyze` vs wwPDB validation report (per-residue `rama=` counts) **✔ partial record resolved (round 45):** re-based on the **42 committed named entries** (`round45_ids.json`; 41 protein, 12CI nucleic), median \|Δ\| **0.000**, p90 0.06, and every per-entry value committed in `round45_vs_deposited.json`. The retired round-17 basis (17 entries from an uncommitted `--ids-file`) is superseded, not re-derived (round-21 caveat: reproduces on named data, does not corroborate the lost set). **Load-bearing check (round 46, #284): per-shared-residue classification agreement**, not the raw %: for the residues both pipelines evaluate, do they assign the same Ramachandran verdict? Observed **1.0000 on all 41 named entries** (keyed with the insertion code — a `(chain, resnum, resname)` key without it collided icode residues, #284 review). The raw favored-% `|Δ| ≤ 0.2 pp` is **retained as a reported diagnostic** (median 0.000; two old/altloc entries — 1ZY2 0.36, 4NJD 0.28 — exceed it for a denominator, not a classification, reason, so they no longer fail the row). `ramachandran_agreement` in `round46_vs_deposited.json`. | `[benchmark]` (`ref/research/tolerance_benchmark_vs_deposited.md`, `tolerance_benchmark_round46.md`) |
| Ramachandran / rotamer outlier % | **Ramachandran: ≤ 0.11 pp** against a wwPDB validation report. The round-17 basis reported "**exact match (Δ = 0), reproduced exactly on 17/17**", but round 45 on the named 41-protein set shows that was specific to the smaller set: median \|Δ\| **0.000**, 33/41 exact, 8 differ by **≤ 0.11 pp** (1VYJ/1ZY2 0.11, 2I4M 0.06, five ≤0.02) — a small denominator/rounding difference between `ramalyze` and the report. The ±0.5 pp band holds with wide margin. **Rotamer: ± 0.5 pp** — round 45 (corrected instrument, #282; the outlier % is now counted from the report's per-residue `rota=OUTLIER` verdicts, not the inconsistent `key_validation_stats` API field) reproduces **exactly on 39/41** entries, but **14ZZ (1.52 pp) and 2YOL (0.57 pp) breach ±0.5** — both carry alternate conformations (altloc A/B), where wwPDB evaluates altloc sidechains `phenix.rotalyze` does not, so the two score a different *number* of residues (14ZZ 459 vs 402) even though every shared residue's rotamer **name agrees exactly** (14ZZ 402/402). The raw % is thus **denominator-sensitive under altloc handling** while the classification agrees. The historical **max 0.34 pp is superseded** (it was measured against the pre-#282 API field and a set with no altloc-heavy entry). The band question — classification agreement vs raw-% agreement — is **#284** | `phenix.ramalyze` / `phenix.rotalyze` vs wwPDB validation report (per-residue `rama=`/`rota=` verdicts) **✔ partial record resolved (round 45 + round 46):** round 45 committed the record (named set `round45_ids.json`, per-entry values), and round 46 settled the band question (#284) by making the **load-bearing check per-shared-residue classification agreement**: the rotamer **OUTLIER verdict agrees on all 41 named entries** (`rotalyze` prints the name field as literal `OUTLIER`, matching the XML `rota="OUTLIER"`), and the Ramachandran verdict agrees **on all 41**. The stricter exact rotamer-*name* agreement is 1.0000 on 40, 0.9919 on 15C8 (three insertion-code residues assigned different names but all Favored — a naming nuance, not an outlier disagreement; named in `rotamer_name_disagreements`). The raw rotamer-% `|Δ| ≤ 0.5 pp` and Ramachandran-% (≤ 0.11 pp observed) are **retained as reported diagnostics**; the two altloc raw-% exceedances (14ZZ 1.52, 2YOL 0.57) are a denominator difference, not a classification one, so they no longer fail the row. `ramachandran_agreement`/`rotamer_agreement` in `round46_vs_deposited.json`. | `[benchmark]` (`ref/research/tolerance_benchmark_vs_deposited.md`, `tolerance_benchmark_round46.md`) |
| Bond-length RMSD | \|Δ\| ≤ **0.008 Å** across **differing** restraint libraries, and only when both tools restrain the **same number of bonds** (only 6/17 benchmark models did; where counts differ ~2×, Δ reached 0.0667 Å — report both figures, not a Δ). The retired ±0.003 Å was exceeded by the *typical* case: median \|Δ\| 0.0040 Å on matched counts, gemmi higher in **17/17**. **Not** the bond-angle mechanism: isolating the library inside one implementation (PHENIX CDL vs Engh & Huber) accounts for only **21 %** of the gap on the models where the bond counts match (9 % across all 17, but that population includes the count-mismatched models this tolerance declares void) — the rest is implementation, so matching libraries does *not* bring the two into agreement. The **matched-library** case is barely tighter: **|Δ| ≤ 0.006 Å**. Note the recipe: the gemmi validator is **`gemmi rmsz`** (its `rmsD` line, in Å), not `gemmi validate` | PHENIX `model_statistics` vs `gemmi rmsz` | `[benchmark]` (`ref/research/tolerance_benchmark_bond_rmsd.md`) |
| Bond-angle RMSD | **matched restraint library required**; ± **0.1°** when both tools use the same library, ± **0.4°** when they differ. The library effect is now measured in-repo by toggling PHENIX's own library (CDL vs Engh & Huber, one implementation): median **0.265°**, max 0.471° — confirming the 0.3–0.4° literature figure, and accounting for **51 %** of the PHENIX-vs-gemmi angle gap — 56 % on matched-count models, i.e. stable — in contrast to bond *length*, where the library is only 9–21 % depending on the subset | PHENIX (CDL default since ~2016) vs `gemmi rmsz` (CCP4 monomer library / Engh & Huber) **Record recovered (round 18):** no per-entry angle value had ever been published — the trail tabulated only the bond columns — so this row's median and max had nothing behind them. Re-run on the committed 17-model set, all 17 process and **every published figure reproduces** (library median 0.2648°, max 0.4711°, 51.1 % share). The per-entry table is now in `ref/research/tolerance_benchmark_round18.md`. **One figure in this row was NOT reproduced**: the *56 % on matched-count models* cannot be recomputed from that run, because the emitted per-entry JSON carries no bond-count-match flag — treat it as unverified until a re-run records the subset. Two facts the aggregate hid: the library effect is **one-signed in 16/17** — CDL always reads lower than Engh & Huber in the same implementation, so this is a directional offset rather than a symmetric spread — and **9PLB's is exactly 0.000°**, so the effect ranges 0.000–0.471° and one model contributes none of it. **Full record for the three figures checked; see the caveat above for the fourth.** | `[template]` `[literature]` `[benchmark]` (`ref/research/tolerance_benchmark_restraint_library.md`) |
| Wilson B | \|Δ\| ≤ **25 %** of the mean **or 2.5 Å², whichever is larger** — and **void when `xtriage` reports ΔB_cart ≥ 25 Å²** (strong anisotropy: the ML and straight-line estimators then diverge without bound and in either direction). Benchmarked on 24 datasets, same MTZ and same intensity columns: median \|Δ\| 13.7 %, p90 27 %. The retired ±**5 Å²** was the wrong *shape* — absolute disagreement scales with B itself (r = 0.81), so a fixed Å² band is vacuous below 1.5 Å (where B ≈ 5 Å²) and violated by 8/24 datasets overall. Treat as **weak corroboration**; for a precise value compare like-method or use the deposition's Table 1 | `xtriage` (ML) vs `ctruncate` (classic) | `[benchmark]` (`ref/research/tolerance_benchmark_wilson_b.md`) |
| L-test ⟨\|L\|⟩ | ± **0.02** and **the same twin/no-twin call**. **Re-measured in round 21 on a committed, re-derivable 24-dataset set** (`bench_t13_wilson_b.py`'s `DEFAULT_SET`; run it, then `bench_t13_l_test.py` over the same cache): median \|Δ\| **0.0065**, **22/24** inside ±0.02, max **0.047**, and the **twin call agreed 24/24**. The original benchmark on 27 datasets — whose set cannot be rebuilt — reported median 0.006, 25/27 inside, max 0.047 and the call agreeing 27/27. **Read that agreement as reproducibility, not corroboration**: the 24 is almost certainly a *subset* of the 27 (both runs show exactly 9 datasets where ctruncate printed no range, and 18 − 15 = 3 = 27 − 24), so it is the same structures through the same two deterministic programs. **Both breaching datasets are identifiable and reproduce exactly**: the original trail names them outright (*"2 / 27 (30IZ +0.030, 9PLC −0.047)"*), and round 21 returns −0.0470 and +0.0300 — as do all five datasets that trail tabulated, to the published digit. The breach evidence survived while the denominator did not, which is **structural rather than lucky**: a worst-N table contains every breach whenever breaches ≤ N. ⚠ **partial record** — the historical 27 cannot be rebuilt, and the round-21 24 re-runs most of it rather than replacing it, so the row is reproducible but not independently corroborated — the call is the load-bearing half, a numeric mismatch with an agreeing call is not a finding. The **"matched resolution range" precondition is unachievable** with default builds (matched in **0 of the 18** datasets where ctruncate reported its range historically, and **0 of 15** in round 21; it restricts the L-test by a median 0.46 Å and exposes no flag to change it), so it is a caveat, not a gate. The full scale is only 0.125 (untwinned 0.500 → perfect twin 0.375), so ±0.02 is ~16 % of range and the observed max (0.047) is 38 %; `xtriage`/`ctruncate` share the Padilla–Yeates *method*, so this checks consistent computation, not method-independence | `xtriage` vs `ctruncate` **⚠ partial record (round 17 audit):** only the 5 worst datasets are tabulated; the other 22 of the 27 are never named, the set is not committed, and it is not the same set as Wilson B's 24 — so the extra datasets cannot be identified even by cross-reference. | `[benchmark]` (`ref/research/tolerance_benchmark_l_test.md`) |
| Completeness (overall) | ± **0.2 pp** vs the validation report's `DataCompleteness` (max observed **0.11 pp** over 9 entries, including one at 62 % completeness — so the tightening is not an artefact of easy cases). The retired ± 1 pp was ~10× too loose. Note the PDBe *experiment* API's `completeness` field is null for every entry tested; use the validation report XML | `phenix.model_vs_data` vs wwPDB validation report | `[benchmark]` (`ref/research/tolerance_benchmark_vs_deposited.md`) |
| Secondary-structure agreement | agent-vs-DSSP three-state ≥ **0.85** over DSSP-assigned residues *(unmeasured — a different comparison)*; two independent assigners: **gate on secondary-structure content, not on agreement**. Three-state agreement is **degenerate at the bad end** — perturbing 1UBQ's coordinates *raises* it (0.75 intact → 0.88 at 1 Å noise → **1.00 at 2 Å**, where both assigners label every residue coil and agree perfectly), so no floor on agreement can work. Require **DSSP H+E content ≥ 0.20** (well-ordered structures 0.46–0.78; perturbed wreckage 0.00–0.11), and given that expect **agreement ≥ 0.65**, fold-class dependent (**α-rich ~0.80–0.85, β-rich ~0.68–0.72**). **High agreement with low SS content is a failure signal, not a pass.** The retired 0.80 floor failed on 12/16 well-ordered structures (ubiquitin, lysozyme, trypsin) while passing the 1 Å and 2 Å wreckage; it had been set from 1SAR (0.865), which scores higher than every entry in the benchmark | agent vs DSSP; DSSP vs biotite P-SEA (`t15_ss_agreement.py`) | `[benchmark]` (`ref/research/tolerance_benchmark_ss_agreement.md`) |
| DockQ score | \|Δ\| ≤ **0.01** (same-implementation noise floor ≈ 0.004; the old ±0.05 was ~12× too loose), and the chain mapping is a **hard gate, not a caveat**: chain-mapping ambiguity is no longer presumed but **proven**, and worth up to **0.79** (~79×) — a plausible mis-mapping took 4HHB and 1BRS from 1.00 to 0.21, i.e. CAPRI *High* → *Incorrect*. **Record the mapping with the score**; a high score does not prove it is right, since symmetric whole-subunit swaps can cost almost nothing (4HHB `CDAB` → 0.98) while partial swaps collapse it. Ambiguity exists only for **homo-oligomers**. The CAPRI-class match is **waived within ±0.03 of a class boundary** (0.23 / 0.49 / 0.80) | agent vs `t16_interface_quality.py` (DockQ) **Record verified (round 18):** re-run on the committed set, every mapping reproduces exactly and `n_mappings_scored` equals the number published (4HHB 4, 1BRS 2; the other three complexes have no equivalent chains). Round 17 had marked this row partial on the reasoning that `plausible_mappings(limit=8)` might have scored mappings that were never printed — `limit` is a cap that was never reached. **Full record.** | `[template]` `[benchmark]` (`ref/research/tolerance_benchmark_selection_sensitivity.md`) |
| Interface buried surface area | \|Δ\| ≤ **3 %** of the mean **or 30 Å², whichever is larger**, with matched 1.4 Å probe, protein-only atom selection, and **PISA's per-side `interface_area` doubled** (biotite reports both sides). Measured on 25 interfaces / 17 entries: median \|Δ\| 1.2 %, p90 2.4 %, max 3.7 %. The disagreement is **one-sided** — biotite reads high in 25/25 — so a *negative* Δ is off-distribution and worth investigating. Relative error is size-driven (median 0.9 % above 1200 Å² total vs 2.3 % below), which is why the absolute floor exists. Chain pairs that are **fragments of one molecule** must be excluded before comparing — they are not interfaces | agent vs biotite SASA (`t16_interface_quality.py`); PISA via the PDBe API | `[benchmark]` (`ref/research/tolerance_benchmark_interface_bsa.md`) |
| NMR ensemble precision (mean Cα RMSF) | \|Δ\| ≤ **0.05 Å**, and the ordered-core **cutoff** must match — not merely the intent to use one. Measured over 5 ensembles: varying only the cutoff (1.0–4.0 Å) moves the value by a median **0.14 Å** and up to **0.84 Å** (3–17× the tolerance); reporting the whole-chain mean instead moves it up to **2.15 Å** (43×). **Report the cutoff with the value.** A whole-chain mean must be reported *alongside* an ordered-core figure, not instead of it | agent vs `t17_nmr_ensemble.py` | `[template]` `[benchmark]` (`ref/research/tolerance_benchmark_selection_sensitivity.md`) |
| R-free vs deposited | **State which reference.** \|Δ\| ≤ **0.02** vs the **deposited** `PDB-Rfree` (max observed 0.0128) — that value came from the depositor's own refinement, software and test set. \|Δ\| ≤ **0.01** vs wwPDB's **recomputed** `DCC_Rfree` (median **0.0000**, max 0.0067; 5/9 entries match to four decimals), which is the same derivation `model_vs_data` performs. The two references differ by more than the tolerance | `phenix.model_vs_data` vs wwPDB validation report; REFMAC5 re-refinement | `[catalog]` `[benchmark]` (`ref/research/tolerance_benchmark_vs_deposited.md`) |
| Independent-code-path R offset | \|Δ R\| ≤ **0.02**, with **matched mask radii** (`gemmi sfcalc --radii-set=cctbx`) and the same work set. Benchmarked on 15 entries (1.20–2.92 Å): median **0.0069**, p90 0.0116, max 0.0151, and **one-sided** — gemmi reads high in 15/15, because PHENIX refits k_iso/k_aniso/k_mask **per resolution bin** while gemmi applies one global scale. Using gemmi's default `vdw` mask instead adds a further median +0.0043 (max +0.014), as much again as the offset itself. gemmi is *not* categorically "simpler" — same flat-mask bulk-solvent and anisotropic scaling model | `gemmi sfcalc` vs `phenix.model_vs_data` | `[benchmark]` (`ref/research/tolerance_benchmark_r_offset.md`) |
| H-placement agreement | H-atom count **identical (Δ = 0) for protein-only models**; **void when non-water hetero components are present** — the PHENIX and standalone `reduce` distributions ship **different het dictionaries** (visible in the `USER  MOD` header, e.g. 30TW `std=5902` vs `std=5155`) and the count then diverges by up to **3.96 %**, exceeding even the original ±2 % on 3/18 models. *(The ±0.1 % published in PR #28 was measured on one builder in two conventions, not on the two builders this tolerance names.)* Asn/Gln/His flip sets: **≤ 10 % of flippable residues may differ** between independent H builders — measured **7.5 %** (48 of 639 residues, 17 models, worst model 16.4 %) for `reduce` vs **`mmtbx.reduce2`**. **Exact match is the wrong requirement**: it holds only for `phenix.reduce` vs standalone `reduce`, which are the same binary (`reduce.4.16.250520`, 0 of 634 disagreements). Run reduce2 with `approach=add add_flip_movers=True` — the flip movers default to **off**, in which case it reports nothing and the comparison looks impossible. Clashscore delta within the clashscore tolerance above. **H-count agreement does not imply H-position agreement**: the count is nearly insensitive to the electron-cloud/nuclear convention that dominates the clashscore, so it must not be reported as evidence that two H builds match | standalone `reduce` vs `phenix.reduce` **✔ partial record resolved (round 47 record + round 48 measure, #287) — history of the mark below (round 17 audit):** the "worst model 16.4 %" figure comes from a 639-residue, 17-model comparison whose members at 0 % are never named, and whose set differs from the sibling benchmarks' 17 ids (it includes 9LK0). `bench_t14_flip_sets.py` commits 12 of the 17, so this figure cannot be recounted. **Round 22 established the round-21 re-measurement route does not transfer here**, and would mislead if attempted: the five missing models are exactly those with **zero** disagreements, so they contribute nothing to the numerator and everything they have to the denominator. Re-running the committed 12 would reproduce all 48 disagreements over fewer than 639 residues and report a **higher** rate than 7.5 % — a discrepancy that is purely an artefact of which models survived. Whether a subset re-run helps depends on *which* members were lost, not how many. **✔ partial record resolved (round 47 record + round 48 measure, #287).** Round 47 re-based the record on the fresh named 42-set (`round45_ids.json`, 41 protein) rather than the lost 17, and round 48 settled the measure: the load-bearing flip-set check is the **genuine confident-conflict rate** — a `reduce`-vs-`reduce2` flip disagreement counts only where `reduce` is confident (`F`/`K`), since category `X`/`C` is one builder declining to commit, not a conflict — which is **56/3105 = 1.80 %** on the named set, well under ≤ 10 %. The **raw** rate (10.95 %, of which 82 % are `reduce`'s own `X` calls) is **retained as a diagnostic**. Four small-n models exceed 10 % on the confident measure (1TIJ 4/20, 1OWJ 2/16, 1BDJ 2/17, 2QTU 2/17 — named, high-variance); the band guards the aggregate, as the retired figure did. `confident_conflicts()` in `bench_t14_flip_sets.py`, `round48_flip_sets.json`. | `[benchmark]` (`ref/research/tolerance_benchmark_clashscore_h.md`) |

> **Method-dependence preconditions (from the domain-expert review — see
> `ref/research/template_tolerance_review.md`).** Two agreement tolerances only hold under a matched
> tool configuration, and comparing without matching will fail for reasons unrelated to a real
> disagreement:
> - **Bond-angle RMSD** depends on the restraint library: PHENIX (CDL) vs gemmi (Engh & Huber / CCP4
>   monomer library) differ by 0.3–0.4° for library reasons alone. Match the restraint library, or use
>   the widened ±0.4° band. **Record the restraint-library and tool versions** with any geometry
>   measurement.
> - **Clashscore** requires a matched hydrogen-build convention (electron-cloud-center for X-ray vs
>   nuclear for neutron/NMR); a mismatch shifts the score (≈ 0.5 on the repo's 1SAR: 3.13 cctbx vs 3.63 standalone — an in-repo observation, not a general benchmark).
>
> **All `[template]` tolerances have now been reviewed** (`ref/research/template_tolerance_review.md`).
> Confirmed defects were fixed: bond-angle (library-conditional), CC½ (citation + 0.1–0.2 floor),
> RSCC (RSZD/RSZO, matched-radius only), aligned-residue count (class-conditional), NMR RMSF
> (matched ordered-core), and the R offset (rationale corrected). Preconditions were added to CA RMSD
> (same selection) and DockQ (fixed chain mapping); Wilson B was loosened to ±5 Å²; L-test, SS
> agreement, bond-length, clashscore, and the Ramachandran/rotamer pp tolerances were kept.
> An independent verified research pass cross-checked all seven of the second batch and confirmed
> these verdicts, additionally tightening **DockQ** to ±0.01 (same-implementation noise floor ≈ 0.004).
>
> **No tolerance is provisional any more.** The two that were — interface BSA and Wilson B — could
> only be settled by running the tools, and now have been (`scripts/bench_t16_bsa_vs_pisa.py`,
> `scripts/bench_t13_wilson_b.py`; audit trails under `ref/research/`). Both changed *shape*, not just
> magnitude: each is now a relative band with an absolute floor, because in both cases the
> disagreement scales with the quantity being measured. **Interface BSA ±10 % → max(3 %, 30 Å²)**
> (~3× tighter on real interfaces; the disagreement is one-sided, biotite high in 25/25).
> **Wilson B ±5 Å² → max(25 %, 2.5 Å²), void under strong anisotropy** — the old band was vacuous
> below 1.5 Å resolution and violated by 8/24 datasets overall.

## 4. Refinement Δ-tolerances (compare→refine flow)

Used by `driving_example.md` when a baseline exists. A refinement must not materially degrade these.

Benchmarked by **re-refining deposited models against their own data** — the null case, where a
correctly-behaving refinement should change almost nothing. The old bands failed that test: 5/8
entries breached the ΔRMSD band and 3/8 the clashscore clause **with no modelling change at all**.
See `ref/research/tolerance_benchmark_refinement_deltas.md`.

> **Detection floor, and what §4 can actually see.** Tested by damaging models with known coordinate
> noise: **degradation below ~0.1 Å Cα RMSD is undetectable by any §4 clause at high resolution**,
> and **below ~0.25 Å at `d_min ≥ 2.5 Å`** (the round-42 band; was ~0.35) — about 2.5× worse, because
> null re-refinement alone moves a low-resolution model up to ~0.20 Å (the 44-entry fresh max; the older
> 0.285 Å came from the lost set). **§4 is therefore primarily a high-resolution check**: above
> 2.5 Å it catches gross damage only (σ ≳ 0.3 Å), the −6 pp favored clause permits a drop from 92 %
> to 86 %, and the absolute §2 quality bars carry most of the weight
> (`tolerance_benchmark_round6.md`, `tolerance_benchmark_round7.md`) — 7 of 9 models damaged at σ = 0.05 Å (≈ 0.09 Å shift) passed
> everything. That is a property of the quantities, not a tuning gap: 0.09 Å sits inside the
> null-refinement spread. At σ = 0.1 Å (≈ 0.17 Å shift) all 9 were caught, by the ΔRMSD band.

| Check | Tolerance | Provenance |
|---|---|---|
| ΔRMSD sanity | **Resolution-conditional.** `d_min < 2.5 Å`: RMSD_post ≤ RMSD_pre + **0.12 Å** (was 0.10, breached by a null re-refinement on 43SK at **0.1011 Å** once the set reached 19 entries below 2.5 Å). `d_min ≥ 2.5 Å`: + **0.25 Å** — the **99 %/95 % lognormal upper tolerance limit** over the 44 fresh named entries (rounds 37/38/41, `analyze_xray_band_coverage.py`, round 42), replacing the retired **+0.35 Å** that was sized just above a lost 0.285 Å max. Validated on **37 entries, 1.37–3.60 Å** (19 below 2.5 Å, of which 11 below 2.0 Å). The band has been widened twice as the set grew: round 8's reported 2× headroom was an artefact of 3 high-resolution entries, round 10 measured 1.15×, and round 11 recorded an outright breach at 0.1011 Å. Re-check whenever entries are added rather than treating it as settled — and that re-check is now **automated (P3c)**: the lognormal assumption behind this UTL is **guarded, not assumed**. `analyze_xray_band_coverage.py` recomputes a **Filliben PPCC normality test** on the current log(Cα) every run (PPCC **0.9856** vs raw 0.9764, critical 0.9735 at n = 44 — log passes and beats raw) and exits non-zero if it fails, and `test_xray_band_coverage.py` fails the gate if log(Cα) stops passing as entries accrue. The underlying dependence is smooth (r = 0.59 with d_min), but a resolution-scaled band is no better for Cα shift and *worse* for favored drop, so the step is kept. A null re-refinement moves a low-resolution model **4× further** than a high-resolution one, so a single band cannot serve both — the earlier +0.05 Å flagged 5/8 correct refinements, and its +0.15 Å replacement (fitted to a 1.37–2.92 Å set) was itself breached by 4/19 once low-resolution entries were included. **✔ partial record resolved (round 42) — this was the most expensive instance in the file.** The old **+0.35 Å** was set just above a **null max of 0.285 Å** from ~11 low-resolution entries **named nowhere**, so the width could not be recounted or re-medianed. Round 42 re-based it: the band is now the **99 %/95 % lognormal upper tolerance limit over the 44 fresh named entries** (rounds 37/38/41), computed by the committed `analyze_xray_band_coverage.py` — a fully-backed figure that flags **0 of the 44** and is well below the old 0.35 Å (which sat 1.75× above the fresh maximum, 0.2004 Å). #225's research established that a band sized on a single lost maximum was a biased, least-robust basis in the first place, so the lost 0.285 Å is retired as the wrong target rather than mourned as lost data | `[benchmark]` (`ref/research/tolerance_benchmark_refinement_deltas.md`) |
| Geometry did not degrade (**Δ clause**) | **favored is resolution-conditional**: `d_min < 2.5 Å` → favored_post ≥ favored_pre − **0.5 pp** (null max 0.25 pp); `d_min ≥ 2.5 Å` → − **6 pp** (null max **5.26 pp**; round 39 reached **6.28 pp** unrestrained on 6LE5 — the first null breach of *this* band since it was set in round 7 — but 2.21 pp under restraints, so the band is **kept**, see the restraint note below, #253). The earlier −1.5 pp was breached by 6/19 null re-refinements once low-resolution entries were included. rotamer outliers_post ≤ outliers_pre + **4 pp** (0/19 breaches, worst null rise 3.60 pp). Bands are quoted for **unrestrained** refinement; adding resolution-appropriate NCS + secondary-structure restraints shrinks the low-resolution null spread — max Cα 0.285 → 0.227 Å and max favored drop 5.26 → 3.35 pp, with a **median** change of −0.007 Å (8/11 improved) and **−1.23 pp** (10/11 improved) — so they are conservative by roughly that much when such restraints were used. **Round 39 confirmed this on a fresh, named set** (the round-38 entries, 14 paired): the unrestrained favored drop reached **6.28 pp** (6LE5, 3.10 Å) — a genuine breach of the −6 pp favored band, which had held since round 7 through round 20's re-measurement (the ΔRMSD and CC_mask bands above have each been breached before; this is the favored band's first) — and the same NCS + secondary-structure restraints brought it to **2.21 pp**, with the set median moving +0.035 → +1.505 pp and **no restrained entry breaching**. The band is **kept at −6 pp**: the breach is specific to *unrestrained* refinement, which is not the protocol used at low resolution, so widening it would loosen the check by re-fitting to a single worst case a real refinement never produces (#253, `tolerance_benchmark_round39.md`). **Clashscore: gate on the ratio, not the difference, and only while `1 ≲ clashscore_pre ≲ 20`** — `clashscore_post / clashscore_pre ≥ 5×` is evidence of degradation; every benchmark model separates its null ratio from its 0.1 Å-damage ratio by ≥ **6.7×**, and no null re-refinement reached 5× — max **4.25×** over the 44 fresh named entries (round 44; the round-7 set gave 4.26× over 19), starting clashscore up to **38.70** (the lost set's 17.2 is exceeded on named data). **The gate is bounded at BOTH ends.** *Above* pre ≈ 20 it fails because damage drives clashscore to ~100 regardless of starting value, so the ratio collapses (100/pre). *Below* pre ≈ 1 it fails the other way, and round 20 found the limiting case: **9LLO starts at clashscore 0.00, so the ratio is undefined and any clash at all reads as infinite degradation** — on a model whose post-refinement clashscore is **0.67**, comfortably inside §2's own ≤ 4 bar. 9LLO is the only entry whose ratio is undefined; two more (30TW, 12LO) have trip points of 5.85 and 5.90, i.e. within 2× that bar — so three of 16 are *fragile* to a small absolute rise, though only one is actually undefined. The fragility is **systematically high-resolution** — median starting clashscore **2.28** below 2.5 Å (n = 12) against **8.96** above it (n = 4) — so well-ordered models are the ones the ratio mis-serves. **Outside roughly 1 ≲ pre ≲ 20, compare the absolute post-clashscore against §2's bar instead.** The exact floor is not measured; the evidence establishes that one is needed, not where it sits. The *difference* stays ungated: on a null re-refinement it moved in both directions (4 up, 4 down, signed median +0.78, range −2.70 to +10.39), so no difference band works. Clashscore is nonetheless the **most sensitive** §4 signal — at 0.05 Å damage it is the only quantity that moves at all. **The X-ray pipeline is reproducible**: round 20 re-refined the original 8 entries and all **8 of 8 reproduced round 5's published Cα shift, clashscore, favored % and rotamer % exactly**, fifteen rounds later. Note the scope — `bench_refinement_deltas.py` has pinned `phenix-2.0-5936` since round 5 and the pin has never moved, so this is **same-binary** reproducibility. Whether a PHENIX upgrade would move these values is **untested**. **✔ partial record resolved (round 42 + round 44).** Round 42 re-based the `d_min ≥ 2.5 Å` favored band: kept at **−6 pp** (round 39 settled its value), now backed as a **nonparametric coverage bound over the 44 fresh named entries**, covering **43 of 44 (97.7 %)** with 6LE5 the single exceedance restraints tame. Round 44 re-based the last partial figures — the clashscore null ratio and starting-clashscore ceiling: over the **44 fresh named entries** the max null ratio is **4.25×** (37 gate-valid, none ≥ 5×; was 4.26× on the lost 19) and the starting clashscore reaches **38.70** with 7 entries above pre = 20 (so the "not reproducible" 17.2 from the lost set is now *exceeded* on named data). The 5× gate is unchanged; only its evidence moved from the lost set to named entries (`analyze_xray_band_coverage.py`). This row is now fully backed. The figures traceable to the original 8-entry table (rotamer worst rise 3.60 pp; clashscore range −2.70 to +10.39) *are* fully recorded; the two that set band widths are not. **Two clauses here have not been re-validated since round 7 (diagnosed round 18).** The ΔRMSD row above is correct at 37 entries (19 below 2.5 Å + 18 above). This row's `19` is not the `< 2.5 Å` branch — the rotamer and clashscore clauses were never resolution-split, so no such branch exists for them. It is round 7's *total*, and **rounds 8, 10 and 11 re-tested only the Cα-shift and favored clauses while growing the set 19 → 26 → 32 → 37.** So `rotamer 0/19` and `no null re-refinement in 19 entries reached 5× (max 4.26×, starting clashscore up to 17.2)` describe a set **18 entries smaller than the one this section claims**, and the underlying worst cases (3.60 pp, 4.26×) come from the original 8. **Round 20 re-measured both clauses** on the **16 identifiable entries** (1.37–2.92 Å): both hold, and both worst cases are **reproduced exactly** by the entries that produced them — 28SW +3.60 pp, 30TW 4.26× — and **not exceeded** by the 8 entries added since round 5, which top out at 1.91×. That is a new measurement with its own denominator, not a re-validation of the 19: the other ~21 entries remain unidentifiable. The quoted **starting clashscore 17.2 was above round 20's recoverable ceiling** — the highest in the recoverable 16 was 13.61 — so it came from a lost entry. But it is **not an unrecoverable extreme**: round 37's fresh sample reached 38.70 (its P5) and round 44's re-basing puts the named ceiling at 38.70, so 17.2 is ordinary for the branch, not a number that can never be checked — an artefact of which entries survived, not of the population. The `6/19` figure is different and is **correct as history**: it records the *retired* −1.5 pp single band failing when round 7 added low-resolution entries, not a claim about the current band | `[benchmark]` (`ref/research/tolerance_benchmark_refinement_deltas.md`) |
| Absolute geometry floors (**split out of the above**) | clashscore ≤ **4**; Ramachandran favored ≥ **97 %**; rotamer outliers ≤ **2 %**. These are **quality bars, not refinement checks** — only 4/8, 4/8 and 6/8 of the *deposited* models in the benchmark meet them. Evaluate and report them separately; never use them to excuse a degradation on an already-poor model | `[literature]` (Chen 2010; Williams 2018 — see §2) |
| Map-model fit did not degrade | **Resolution-conditional.** `d_min < 3.0 Å`: CC_mask_post ≥ CC_mask_pre − **0.04** (14 entries, null min **−0.0311**; was −0.02, breached by 9O9K at 2.90 Å). `d_min ≥ 3.0 Å`: − **0.06** (14 entries, null min −0.0475). **69 EM entries, 2.40–4.11 Å — but only ~17–22 of them are evidence.** Those denominators are different counts of the same file and each is stated so a later round cannot let them drift: **69** entries that entered the refinement benchmark (excluding the 4 unidentified `LOST` rows **and** the 24 `screened only` rows round 23 measured pre-refinement and never refined — both are in the file, neither is a denominator here), of which **59** reached a refinement attempt, **58** carry a recorded Δ and **35** have full pre/post values. (**63** entries reached a refinement attempt in total — the extra 4 are the `LOST` rows, measured by round 13 and excluded above because their identities are gone. They nest under the file, not under the 69.) Earlier rounds published **53**, which was the refinement-attempt count before round 19 — the headline switched to the named count here, so it grew by 16 while the set grew by 10. Only a *degradation* can breach a `post ≥ pre − x` band; an improvement cannot fail it at any size. Across all entries with published per-entry values, **17 degraded — a lower bound, not a count**: 9 of the 22 in round 12's table, 9O9K in round 13, 0 of 8 in round 14, 4 of 8 in round 15, 1 of 9 in round 16 and 2 of 10 in round 19. Round 13 published only its branch minimum for the 5 other entries it added, so the true figure is **17–22**. Per-entry values from round 14 onward are recorded in `ref/research/data/em_refinement_deltas.tsv`; earlier rounds' are partly unrecoverable. **⚠ partial record (round 17 audit)** — but the *visible* kind: this row publishes its own uncertainty as a range rather than a point, which is what the other marked rows cannot do. **The degradation *rate* is not measurable at this sample size — do not try to explain it.** Round 15 (3.00–3.90 Å) degraded 4 of 8 and round 16 (3.00–4.11 Å, coarser) 1 of 9, which looks like a four-fold rate difference; round 17 pre-registered a mechanism hunt for it and **the gate failed first — Fisher's exact test on 4/8 vs 1/9 gives p = 0.131.** Holding those rates, the comparison first clears p < 0.05 at **20 entries per round**, and rounds carry 8–9. A post-hoc 3-round permutation test (200 000 permutations, n = 25) gives p = 0.048 for rate homogeneity, which is marginal, post-hoc, and driven mostly by round 14's 0/8. **A rate question here needs ~20 entries per arm or it should not be asked.** Magnitude remains the quantity a band can be fitted to. **The pipeline is deterministic** — `real_space_refine` re-run on 32FE reproduced its CC_mask exactly at 4 dp, and round 17 re-ran 10BU from a clean directory two days later and got a **byte-identical refined model** (same MD5), so the finding rests on two entries — every recorded Δ is exact and no value is a noise artefact, however small. Magnitude is resolution-linked — Spearman ρ(resolution, |Δ|) = **+0.361** over the **58** entries with a recorded Δ (p = 0.005, as of round 19; round 16 measured +0.397 at n = 44) — but not monotonically — 3.0–3.5 Å carries the largest *median* excursion, 3.5–4.2 Å the largest *single* one. Degradation is **more frequent below 3.0 Å (4/8) than above it**, while being far *smaller* there (≤ 0.0139 versus ≥ 0.0217): frequency and magnitude point at opposite resolutions, and the band width tracks magnitude. Round 14's 8 entries at 2.40–3.10 Å degraded **0** times; round 15's 8 entries at 3.00–3.90 Å degraded **4** times (50 %, worst −0.0351), confirming that the low-resolution regime is where degradations large enough to re-fit a band occur. **Publication clustering was tested and does not predict a similar null Δ** (within-cluster mean |Δ difference| 0.0318 vs 0.0354 between, permutation p = 0.38), so entries from one paper are counted independently. Quote the degradation count, not the entry count: adding entries that improve raises the latter and leaves the former untouched. The 3.0 Å split is conservative — every entry from 2.97 to 3.07 Å is positive and the first excursion past −0.02 is at 3.08 Å — and it is **kept** despite the branches differing by only 1.5×. Round 14 reversed an earlier recommendation to collapse it: a single −0.06 band would indeed hold on every entry measured, but it loosens the `< 3.0 Å` branch by 50 % exactly where the null spread is tighter (worst −0.0311, versus −0.0475 above 3.0 Å), giving 1.93× headroom and blindness to a real 0.05 drop at 2.4 Å. **Headroom against breaching is not the criterion — detection power is**; a band of ±∞ maximises the former. Resolution **bounds** the excursion only loosely, and less well than round 12 stated: that round recorded "every large negative Δ is at ≥ 3.08 Å and all 7 entries below 3.0 Å stay within −0.0139", which 9O9K (2.90 Å, −0.0311) then contradicted. It does **not predict** the excursion either — at ≥ 3.0 Å entries degrade and improve in roughly equal numbers, and no alternative predictor was found (atom count, model-to-map coverage, starting CC_mask). **Starting CC_mask was re-tested under pre-registration in round 17 and fails**; figures below are **re-derived on the current n = 35** (round 17 published them at n = 25, before round 19's entries). It does not separate degraders from improvers (Mann–Whitney **p = 0.18**), and the round-level means run backwards — round 14 started with the *highest* mean CC_mask (0.8302) and degraded **0 of 8**, while round 15 started lowest (0.7625) and degraded 4 of 8. A negative ρ(pre, Δ) = **−0.506, p = 0.0019** does appear — and it **strengthened** as the set grew — but it still does not survive Oldham's correction (ρ(mean, Δ) = **−0.281, p = 0.10**), still fails leave-one-out (weakest −0.224 against a 0.339 critical value), and ρ(post, Δ) is **−0.006**. That is the signature of correlating a change against its own baseline rather than of an effect, now confirmed on 40 % more data than the round that first diagnosed it: **the raw correlation grows while the corrected one does not.** Treat the split as an envelope, not a mechanism. This band has broken three times as the set grew: −0.01 (set on 2 entries) breached at 6; −0.02 breached by 3 of 13; and the `< 3.0 Å` branch's −0.02 (set on 8 entries from one worst case) breached at 14. Each new band is again set just above a single worst case, so treat a further break as the base case. **`d_FSC_model` IS gateable — but not via mtriage's reported value.** `d_FSC_model_post ≤ d_FSC_model_pre + 0.05 Å`, with the crossing computed from `fsc_model.masked.mtriage.log` as the resolution beyond which FSC stays below 0.143 for **20 consecutive shells**. Neither naive rule is safe: the **first** crossing (what mtriage reports) is defeated by one anomalous low-resolution shell, and the **last** crossing by oscillation in the high-resolution tail — on 27WR it returns 2.24 Å for a 2.70 Å map. mtriage reports the **first** shell below the threshold, and a single anomalous low-resolution shell defeats it — 9VJD's curve dips to FSC 0.073 at 23.11 Å and recovers, so mtriage returns 23.11 Å for a 2.86 Å map (last crossing: **2.77 Å**). The bug also hits entries whose 0.143 value looks fine: 21BQ reports 36.11 Å at FSC = 0.5 against a true 2.69 Å. Half-maps do **not** fix it (tested). Supplying both half-maps makes the *half-map* FSC work (27WR: d_fsc 2.61 Å) while leaving the *model-map* crossings degenerate; a coverage explanation was also proposed and withdrawn (21BQ works at 71 atoms per 10⁶ Å³, within 2.6× of the failing 9VJD's 27). The band is retained at ± 0.05 Å: **one-sided**: `d_FSC_model_post ≤ d_FSC_model_pre × 1.05`. Larger `d_FSC_model` is worse, and the clause is "did not degrade", so improvements are unbounded — over **69** entries the largest *degradation* is **+4.786 %** (10BU, 3.20 Å) with **0 breaches** — leaving only **1.0448× headroom**, the thinnest margin in this file. **10BU was re-run in round 17 and reproduces byte-identically** (same refined-model MD5, same CC_mask, same FSC crossings to full float precision), so the entry holding up this band is verified, not assumed. The figure long quoted as **+4.787 %** was a **backfill artefact** — round 16 reconstructed rounds 14–15 from published values already rounded to 4 dp and recomputed the ratio from those; the pipeline's own value is +4.7856 %. All 16 rounds-14–15 `d_FSC_model` values have been refreshed from the surviving mtriage curves (max change 0.0024 pp) and 21AO, previously blank, was recovered at 2.8125 Å unchanged. Round 16 targeted this band with 9 entries at 3.00–4.11 Å and produced 4 degradations, worst **+1.476 %** (10ME): the band held with 3.4× headroom against that round's worst. **Round 19 then targeted 10BU's own window** — 10 fresh entries at 3.05–3.45 Å, from 10 distinct publications — and produced 2 degradations, worst **+0.277 %**, an **18× margin**. That falsified a registered prediction that the largest would exceed 1.1 %, and it locates 10BU sharply: it now stands **3.24× above the next-largest degradation ever recorded and 30.6× the median**, and its own window has been sampled 22 times with a second-worst of +0.277 % — **17× smaller**. **Do not tighten the band on that basis.** Round 17 re-ran 10BU from a clean directory and it reproduced byte-identically, so it is a real degradation a tighter band would fail on at once. The band's position is simply now known precisely: `× 1.05` sits 1.0448× above one verified extreme and ~18× above everything else. **⚠ partial record (round 17 audit):** of the **8 degradation magnitudes on record**, the median is 0.157 % and only one exceeds 4 % — so the large excursions are rare rather than routine, and an earlier reading that put 2 of 3 above 4 % was drawing on a **publication-biased sample** in which only the alarming values had been written down. By the same counting argument the band rests on its **8 recorded degradations**, not on the **36** measurements that produced them — 14 if round 13's unpublished ones are counted, which is a firmer figure than CC_mask's equivalent because round 13 did at least publish its `d_FSC_model` direction split. round 14 added 8 entries and 0 degradations, round 15 added 8 and 2, round 19 added 10 and 2. Note also that CC_mask and `d_FSC_model` can move in **opposite directions** on the same refinement (10RI: CC_mask +0.0115 while `d_FSC_model` degraded +0.444 %), so the two clauses are not interchangeable evidence, while the largest *change* is a **36 % improvement** (9H7U, 4.06 → 2.59 Å) that a two-sided reading would have failed. The ± 0.05 Å **absolute** band it replaces was genuinely breached, but by 1 entry rather than the 3 first reported — the other two were improvements counted as failures. The quantity ranges **2.06–4.35 Å** across the 36 benchmark entries with a recorded crossing (1.87 Å over all 60 including round 23's screened set), so no absolute band serves both ends — one wide enough to cover the data (0.30 Å) would permit an 11 % change on a 2.7 Å measurement. **Caveat — an untested hypothesis resting on n = 2, not a rule: the Δ *may* be least trustworthy when the crossing starts far beyond the map's own resolution.** `d_FSC_model_pre / d_min` exceeds **1.3** for exactly two entries on record — 9H7U (1.372) and 10BU (1.360) — and those are the **two largest excursions in the whole set**, −36.15 % and +4.786 %, in opposite directions. Median |Δ| is **20.5 %** above that ratio against **0.112 %** below it, and the ratio distribution has a clean gap (1.372, 1.360, then 1.076). The correlation over all 36 entries is ρ = +0.346, p = 0.039 — found *against* the arithmetic, since a larger `pre` inflates the outcome's own denominator. **But it is n = 2**: it fails on removing either entry (ρ = +0.288, p = 0.094), the 1.3 split is post-hoc, and 10ME contradicts it (4th-largest excursion, 29th of 36 on the ratio). Round 8's rule applies — a mechanism from two data points is a hypothesis. **Round 23 ran the successor test and could not complete it**: screening 24 fresh entries on that pre-refinement ratio found **zero** above 1.3, which is a one-in-four outcome at the 5.6 % base rate and consistent with it (Fisher p = 0.512). The hypothesis is therefore **untested — neither supported nor refuted** — and needs ~60–90 screened entries to yield the 3 candidates a powered comparison requires. **What round 23 did establish**, over the **60** crossings now measured: median ratio **0.9843** with **50 of 60 (83 %) between 0.73 and 1.01** and a right-skewed tail, so the sustained crossing tracks the map's stated resolution closely in the ordinary case. The outlier fence is **1.074**, not 1.3, and **4 of 60** clear it — one of which, **10EU (ratio 1.076), was already refined and moved only −1.084 %**, just under the 10× bar the hypothesis predicts. So the hypothesis is untested at 1.3 and has one near-miss at 1.074. **Round 40 answered it by measuring the mechanism instead of screening for it (#224).** The *ratio* is 2-point leverage — over the same 19-entry set its Spearman ρ with |excursion| collapses from +0.319 to **+0.049** once 9H7U and 10BU are removed — but **crossing determinacy, measured as how far a fixed 0.1–0.3 Å coordinate perturbation moves the sustained crossing, predicts |excursion| robustly**: ρ = +0.505 over 19, **+0.792 with the extremes removed** (leave-one-out 0.75–0.85), and **partial ρ = +0.773 controlling for cc_mask_pre** — so it is not the #234 fit confound. The large excursions are **real model movement, not estimator jitter**: a controlled perturbation moves the crossing 2–2900× *less* than refinement did (9H7U's 36.15 % excursion against a 0.012 % perturbation shift at σ = 0.2 Å), consistent with 10BU reproducing byte-identically. So the caveat is real but the ratio was the wrong measure of it — treat a crossing whose position is perturbation-sensitive as the one whose Δ will be large, and a large Δ there as a genuine degradation to check. The n = 2 ratio hypothesis is **retired** (`tolerance_benchmark_round40.md`, `scripts/bench_dfsc_determinacy.py`). **Caveat: the range's upper end rests on one entry whose value is not in the per-entry TSV** (9VAM: its row there is `delta-only`, but `ref/research/tolerance_benchmark_round12.md` records the measurement in full — 6.1020 → 6.3629 Å, Δ +0.2609 — so the figure **is** checkable, just not by `check_registry_figures.py`, which reads only the TSV. The largest crossing in the TSV is 4.35 Å, 10BU) — without it an absolute 0.15 Å band would also pass, so the evidence for the *shape* is thinner than for the number. A `|Δd|/d²` band, which the curve's uniform 1/d sampling suggests, was tested and fits *worse* (max/median 15.4 vs 14.0). The three large excursions are genuine curve movement, not estimator jitter: 124, 399 and 117 shells respectively — but note only **one of the three is a degradation** (9VAM); the other two are improvements, so this evidence supports the size of the movement, not a breach count, and that is an **upper bound** — part of each Δ is the sustained estimator's shell quantisation, not model movement (27WR's Δ is ~2 shells) | `[benchmark]` (`ref/research/tolerance_benchmark_refinement_deltas.md`) |

## 5. Calibration checks

Sanity checks against known-good cases; a failure exposes a pipeline bug, not a model defect.

| Check | Expectation | Provenance |
|---|---|---|
| Identity superposition | `1UBQ` onto itself → CA RMSD **0.00 Å**, full-length alignment | `[calibration]` |
| Clean geometry baseline | `3NIR` (0.48 Å) → clashscore ≤ **2**, Ramachandran outliers = **0** | `[calibration]` |
| Resolution calibration | reference-model d_FSC_model within **0.10 Å** of the EMDB-header resolution | `[calibration]` |
| Merged-only data honesty | CC½, ⟨I/σ⟩-outer, R-merge/R-meas reported **unavailable** (not fabricated) when unmerged intensities are absent; `T13_aimless_status` records the abort | `[oracle_tools]` `[catalog]` |

## 6. Negative-control verdict rules (gold-standard starts as negative tests)

Used by the negative-control series (`ref/research/negative_control_benchmark_plan.md`, tracker
#295): a sub-Å gold standard is the *start*, a refinement is the subject, and any change from the
pristine start is a candidate degradation graded by **cross-tool agreement** — never by one tool
family, and never PHENIX grading PHENIX. The machine-readable source for every figure in this
section is `scripts/bench_recover_leg.py` (`REGISTERED_FIT_THRESHOLDS`,
`REGISTERED_FIT_THRESHOLDS_ANIS`, `CANDIDATE_LEG_THIRD_OPINION_STANDDOWN`). The threshold tables are
re-derived **on demand** from the committed round record named in their rows (`fit_thresholds_from_record`,
`anis_thresholds_from_record`, called by the drivers and by validate 3b, each asserting the literal equals
the record); the stand-down set is registered by hand from the round-11 L2 else-branch.
`scripts/check_negative_control_records.py` (validate 3b) asserts this section restates those constants
exactly — the negative-control clause of `CODING_STANDARDS.md` rule 10. Drivers cite this section and
import the constants; they do not restate the numbers.

| Rule | Registered form | Provenance |
|---|---|---|
| Family flags | Four per entry and subject. **F-data**: ΔR-free > +3·S_r2 on **both** registered R paths (`phenix.model_vs_data`, `gemmi_rfactor`), S_r2 = round-2 measured noise scales **0.00275 / 0.00260**; REFMAC sign disagreement is a named cross-tool conflict and the flag stands down. **F-geom**: the §4 geometry clause, d_min < 2.5 Å branch, in its bounded form (favored − 0.5 pp, or rotamer + 4 pp, or the bounded clashscore clause), via the cctbx trio with REFMAC zBOND as confirmation. **F-protected**: ≥ 1 protected-outlier fix. **F-shift**: unmasked Cα shift > **0.12 Å** (§4 ΔRMSD band; conservative toward not flagging on the unmasked-only shift) | `[benchmark]` round 3 B3 (`negative_control_round3_preregistration.md`; S_r2 from `negative_control_round2_screen.json`) |
| DEGRADED | **≥ 2 families** flagged. Precedence: DEGRADED > FIT-DEGRADED > not-degraded; a fit verdict never overrides a two-family one | `[benchmark]` round 3 B3; `bench_recover_leg.combined_verdict` |
| FIT-DEGRADED (E1 fit rule) | Positive evidence always: **all three** R residuals past their null-centered thresholds when REFMAC is measurable; the **two R paths standing alone** when it is not. Never "unknown" — a missing third opinion narrows the evidence, it does not manufacture a verdict either way. Thresholds are `median_null + 3·MAD_null` per tool, MAD floored at **0.0005** | `[benchmark]` round 4 C1 → round 5 E1 (`negative_control_round5_preregistration.md`) |
| FIT thresholds — ISOT convention (history, rounds 3–8) | d_phenix **0.01200**, d_gemmi **0.01025**, d_refmac **0.00560** (round-7 H1; the round-4 values 0.01220 / 0.01090 / 0.00540 were retired when the round-6 U3 recompute moved the table on 8R5K's clean null: n = 22 on the R paths, 20 on REFMAC). REFMAC's default `BREF ISOT` **collapses deposited anisotropic ADPs**, so this d_refmac measures a convention, not the model; retained only so rounds 3–8 reproduce | `[benchmark]` `negative_control_round6_hygiene.json` (`u3_c1_recomputed`), `negative_control_round7_preregistration.md` H1 |
| FIT thresholds — ANIS convention (current, round 9 on) | d_phenix **0.01200**, d_gemmi **0.01025** (unchanged — REFMAC's ADP convention does not touch the two R paths), d_refmac **0.01150** from the round-9 null with `REFI BREF ANIS` on both sides of every delta (n = 21, median \|Δ\| 0.0051). **No-mixing rule:** the two conventions are never compared inside one delta; a driver that measures under ANIS grades under the ANIS table only | `[benchmark]` `negative_control_round9_anis.json` (`j2_anis`), rounds 7–8 attribution (`negative_control_round7.md`, `negative_control_round8.md`: the aniso tax is set-wide, 21/21) |
| W4 — no success contradicts its own evidence | A row graded a success may not carry any measurable R residual above **2×** its threshold; a violation fails the round, not the row. One-sided (positive residuals on a success row), computed on the same numbers the verdict used | `[benchmark]` round 5 W4 (`bench_round5.w4_contradiction`); falsified on 2VXN at n = 1 in round 5 (`negative_control_round5.md`) and again in round 10 (Y2's W4 clause, `w4_contradictions: 1`). The letter is reused: round 8's "W4" is the unrelated 9YGW PDB-form prediction |
| Candidate-leg third-opinion stand-down | Entries whose **deposited** baseline carries a registered cross-family conflict contribute **no REFMAC third opinion on candidate legs**: their pre-anchored REFMAC deltas are incommensurable (round-11 echo: on the executed `osol_h` leg REFMAC's post sits 0.0009 from the two-path mean while the pres differ by 0.0328 = solvent 0.0410 − riding-H 0.0111 + 0.0029; the round-10 three-way post spread that motivated the round was 0.0012, `negative_control_round11_preregistration.md`). The post-agreement amendment was **refused** by its own registered L3 safety sweep (54 of 78 rule flips). Set: **{2VXN}**; membership, not equality, is what drivers test | `[benchmark]` round 11 L2/L3 (`negative_control_round11_echo.json`, `negative_control_round11.md`) |
| Screen enrollment (headroom) | An entry enrolls only when the null re-refinement shows **no cross-tool-agreed headroom**; the D6 statistic is mask-constrained (`screen_round1.d6_statistics(min_unmasked_shift=…)`), with the shift floor bound at the next screen registration, not here | `[benchmark]` rounds 1–2 (`negative_control_round2_screen.json`); #321 |

Two invariants sit above the table. A **falsified prediction stays falsified**: committed rounds are
never re-judged, only disclosed — every falsified prediction in the series is reported as registered
(round-1 P4, round-3 Q2/Q3, round-5 W4, round-6 U3, round-7 V3/V4, round-10 Y2 and round-11 Z3
among them; prediction letters are reused across rounds, so a letter names nothing without its round). And a **threshold changes only by re-registration** from a committed null record, with
the record-vs-registration assertion (`fit_thresholds_from_record`, `anis_thresholds_from_record`)
proving the new table before any verdict uses it.

---

## Adding a threshold

1. Add the row here first, with a `[provenance]` tag and — for `[literature]` — a full citation.
2. Cite this file from the driving example that uses it; do not restate the number there.
3. A threshold with no admissible provenance is not admissible (see `CODING_STANDARDS.md` rule 10).
