# Thresholds and standards registry

The single source of truth for every numeric threshold the harness scores against. The per-task
driving examples (`driving_example_T<NN>.md`) and the combined `driving_example.md` cite this file
rather than restating values, so a threshold is defined once and audited once.

Each entry names its **provenance**, which also says how freely it may be changed:

| Tag | Meaning | Change policy |
|---|---|---|
| `[schema]` | A definition committed in `schemas/protstruct_review.yaml` (`ResidueOutlierKind`). | Change only with the schema. |
| `[MolProbity]` | Richardson-lab Top8000 percentile standard. | Tracks the upstream standard. |
| `[literature]` | A published threshold (cited in full below). | Tracks the cited paper. |
| `[catalog]` | A value stated in `ref/catalog.yaml`. | Change with the catalog. |
| `[template]` | A cross-tool **agreement tolerance** (how close two tools must land). | Safe to tune with evidence. |
| `[calibration]` | A sanity check against a known-good case or a deposition. | Safe to tune with evidence. |
| `[benchmark]` | An agreement tolerance **measured in this repo** by running both tools over a test set (audit trail under `ref/research/`, script under `scripts/bench_*.py`). | Change by re-running the benchmark, not by argument. |

`[template]` and `[calibration]` values are engineering tolerances, not scientific claims — tune them
as the harness accumulates evidence. `[schema]`, `[MolProbity]`, and `[literature]` values track
external standards — change them only when the standard does, and update the citation.
`[benchmark]` is the strongest tag available for a cross-tool tolerance: unlike `[template]`, the
magnitude was observed rather than inferred.

---

## 1. Standard outlier definitions

Committed in `schemas/protstruct_review.yaml::ResidueOutlierKind`; percentile bases are the
Richardson-lab Top8000 reference set (high-resolution ≤ 2.0 Å structures).

| Outlier | Definition | Provenance |
|---|---|---|
| Ramachandran outlier | backbone φ,ψ outside the **99.95th** percentile region | `[schema]` `[MolProbity]` |
| Rotamer outlier | side-chain χ outside the **98th** percentile favored region | `[schema]` `[MolProbity]` |
| Cβ deviation | > **0.25 Å** from ideal | `[schema]` |
| Clash | steric overlap ≥ **0.4 Å** | `[schema]` |
| Bad geometry | bond / angle / planarity > **4σ** | `[schema]` |
| Cis-peptide | non-Pro cis ω | `[schema]` |

> **Reference-set caveat.** Top8000 percentiles were built from ≤ 2.0 Å structures. Applying them to
> a lower-resolution model still computes a percentile, but the outlier bar was set against tighter
> geometry than that model can deliver — note it when a model is well below 2.0 Å.

## 2. Literature thresholds

| Quantity | Threshold | Source |
|---|---|---|
| TM-score fold call | **> 0.5** ≈ same fold; **< 0.17** ≈ random pair | Xu J, Zhang Y. *Bioinformatics* 2010; 26(7):889–895. `[literature]` |
| CC½ resolution-cutoff floor | high-resolution shell is usable down to **CC½ ≈ 0.1–0.2** (significance is sample-size-dependent: CC > 0.3 significant at n > 100, CC > 0.08 at n > 1000 — prefer a per-shell significance test or paired refinement over a fixed floor) | CC½/CC\* definition: Karplus & Diederichs. *Science* 2012; 336(6084):1030–1033. Cutoff value: **Diederichs & Karplus. *Acta Cryst.* D 2013; 69(7):1215–1222** ("Better models by discarding data?"). `[literature]` |
| L-test twinning | untwinned ⟨\|L\|⟩ ≈ **0.5**; perfect twin ≈ **0.375** | Padilla JE, Yeates TO. *Acta Cryst.* D 2003; 59:1124–1130. `[literature]` |
| CAPRI class from DockQ | High **≥ 0.80**; Medium **[0.49, 0.80)**; Acceptable **[0.23, 0.49)**; Incorrect **< 0.23** | Basu S, Wallner B. *PLOS ONE* 2016; 11(8):e0161879. `[literature]` |
| pLDDT confidence cutoff | per-residue **pLDDT ≥ 70** = confident (trim below); ≥ 90 = very high | Jumper J et al. *Nature* 2021; 596:583–589. `[literature]` |
| Phaser MR solution | translation-function **TFZ > 8** = confident/definitely-solved; 5–8 ambiguous | McCoy AJ et al. *J. Appl. Cryst.* 2007; 40:658–674. `[literature]` |
| Real-space density fit (ligand/loop) | **Accuracy: RSZD** significant at **±3σ** (misplaced atoms / unexplained density); **precision: RSZO** floor **~1σ**. RSCC/RSR have *no B-factor-independent significance criterion* (radius convention alone shifts them "wildly"), so use RSCC only as a corroboration signal with a **matched limiting-radius convention**, not as a fixed ≥0.8 bar or a fixed ±0.05 agreement tolerance | RSZD/RSZO via EDSTATS / PDB-REDO `density-fitness`; RSCC only matched-radius | Tickle IJ. *Acta Cryst.* D 2012; 68(4):454–467. `[literature]` |
| Cryo-EM map resolution (FSC) | gold-standard FSC threshold **0.143** (half-maps); model-map FSC **0.5** | Rosenthal PB, Henderson R. *J. Mol. Biol.* 2003; 333(4):721–745. `[literature]` |

## 3. Cross-tool agreement tolerances

How close the agent's tool and the independent oracle must land on the **same** input. These grade
agreement, not quality.

| Metric | Tolerance | Tools | Provenance |
|---|---|---|---|
| CA RMSD | \|Δ\| ≤ **0.03 Å**, and **only when both aligners report the same aligned-residue count** — if the counts differ, even by one, the RMSDs are over different atom sets and must not be compared; report both with their counts. Benchmarked on 20 deposited pairs incl. homologs: on matched selections max \|Δ\| **0.02 Å** (13/20 pairs); on unmatched, up to **0.66 Å**. A single-residue difference (1MBN/1MBO, 153 vs 152) alone moved RMSD by 0.15 Å. Run `phenix.superpose_models` with `morph=False trim=False` — its defaults deform the moving model, and TM-align is rigid-body | PHENIX `superpose_models` vs TM-align (`-ter 0`) | `[benchmark]` (`ref/research/tolerance_benchmark_superposition.md`) |
| Aligned-residue count | ± **2 residues**, **only within one aligner class** (both structure-based, or both sequence-based; TM-align/US-align drop distant Cα pairs by design while LSQ aligners keep the full alignment) **and with matched chain handling** — TM-align stops at the first `TER` and aligns the **first chain only** unless given `-ter 0`, which on 1A2P/1BNI is the difference between ΔN = 185 and ΔN = 31. Confirmed on 20 pairs: 13 exact, 2 off by one, 1 off by two. A larger difference is a real disagreement about the alignment — inspect it, do not widen the band | same class only, matched chain handling | `[benchmark]` (`ref/research/tolerance_benchmark_superposition.md`) |
| Clashscore | \|Δ\| ≤ **1.0**, or **20 % of the mean, whichever is larger**, **with a matched H-build convention**. Benchmarked on 10 models (clashscore 1.2–13.6): matched, median \|Δ\| **0.115**, max 2.27 (17 % on the worst model). **Mismatched** (nuclear vs electron-cloud H), median **9.95**, max **22.97** — 10–23× the tolerance, so that comparison is **void, not failed** | PHENIX/cctbx vs MolProbity standalone (`reduce` + `probe`) | `[benchmark]` (`ref/research/tolerance_benchmark_clashscore_h.md`) |
| Ramachandran / rotamer favored % | **Ramachandran: ± 0.2 pp** — measured against the wwPDB validation report over 17 entries (median **0.00 pp**, p90 0.02, max **0.16 pp**), so ± 1.0 pp was ~6× too loose. Unlike the *outlier* row this evidence is informative on all 17 entries, since favored % is never degenerate. **Rotamer favored %: ± 1.0 pp retained, not directly measurable** — the report's `rota=` attribute is the rotamer *name* (`m-10`, `mp`, …), not a favored/allowed verdict, so no reference figure exists. The underlying **assignment** is reproduced exactly, however: **8054 of 8054 residues** across 17 entries get the identical rotamer name from `phenix.rotalyze` and the wwPDB report. The **chi geometry is verified exact** — chi1 computed independently with gemmi matches `phenix.rotalyze` to ≤ **0.05°** (its printed precision) over **8054 residues**, so chi computation contributes nothing to any disagreement. The **library** is corroborated too, by a genuinely independent one: the CCP4 monomer library's own chi torsion targets, scored via `gemmi rmsz`, rank sidechains the same way — median torsion |Z| **1.50 / 3.30 / 4.33** for MolProbity Favored / Allowed / OUTLIER over 8054 sidechains. The two answer different questions (density percentile vs deviation from a target torsion) so they cannot be differenced, but the band no longer rests on an untested shared-library assumption. The favored/allowed boundary itself is **bounded rather than verified**: **3.9 %** of residues sit within ×1.25 of the 2 % Favored/Allowed cutoff, so a systematic ±25 % scoring difference could move favored % by up to 3.9 pp — above the band. ± 1.0 pp therefore **assumes the two pipelines share a rotamer library**, which the identical assignment supports but does not prove | `phenix.ramalyze` vs wwPDB validation report (per-residue `rama=` counts) | `[benchmark]` (`ref/research/tolerance_benchmark_vs_deposited.md`) |
| Ramachandran / rotamer outlier % | **Ramachandran: exact match (Δ = 0)** against a wwPDB validation report — reproduced exactly on 17/17 entries, though **13 of those compare 0.00 to 0.00**, so the informative evidence is the **4 entries with nonzero outliers** (1.03, 1.06, 0.70, 0.09 — all exact, at 2 dp reported precision). ± 0.5 pp was unlimited headroom; any difference is worth investigating. **Rotamer: ± 0.5 pp** — confirmed, max observed **0.34 pp** (rotamer classification is the looser of the two: it depends on sidechain completeness and altloc handling that differ slightly between pipelines) | `phenix.ramalyze` / `phenix.rotalyze` vs wwPDB validation report | `[benchmark]` (`ref/research/tolerance_benchmark_vs_deposited.md`) |
| Bond-length RMSD | \|Δ\| ≤ **0.008 Å** across **differing** restraint libraries, and only when both tools restrain the **same number of bonds** (only 6/17 benchmark models did; where counts differ ~2×, Δ reached 0.0667 Å — report both figures, not a Δ). The retired ±0.003 Å was exceeded by the *typical* case: median \|Δ\| 0.0040 Å on matched counts, gemmi higher in **17/17**. **Not** the bond-angle mechanism: isolating the library inside one implementation (PHENIX CDL vs Engh & Huber) accounts for only **21 %** of the gap on the models where the bond counts match (9 % across all 17, but that population includes the count-mismatched models this tolerance declares void) — the rest is implementation, so matching libraries does *not* bring the two into agreement. The **matched-library** case is barely tighter: **|Δ| ≤ 0.006 Å**. Note the recipe: the gemmi validator is **`gemmi rmsz`** (its `rmsD` line, in Å), not `gemmi validate` | PHENIX `model_statistics` vs `gemmi rmsz` | `[benchmark]` (`ref/research/tolerance_benchmark_bond_rmsd.md`) |
| Bond-angle RMSD | **matched restraint library required**; ± **0.1°** when both tools use the same library, ± **0.4°** when they differ. The library effect is now measured in-repo by toggling PHENIX's own library (CDL vs Engh & Huber, one implementation): median **0.265°**, max 0.471° — confirming the 0.3–0.4° literature figure, and accounting for **51 %** of the PHENIX-vs-gemmi angle gap — 56 % on matched-count models, i.e. stable — in contrast to bond *length*, where the library is only 9–21 % depending on the subset | PHENIX (CDL default since ~2016) vs `gemmi rmsz` (CCP4 monomer library / Engh & Huber) | `[template]` `[literature]` `[benchmark]` (`ref/research/tolerance_benchmark_restraint_library.md`) |
| Wilson B | \|Δ\| ≤ **25 %** of the mean **or 2.5 Å², whichever is larger** — and **void when `xtriage` reports ΔB_cart ≥ 25 Å²** (strong anisotropy: the ML and straight-line estimators then diverge without bound and in either direction). Benchmarked on 24 datasets, same MTZ and same intensity columns: median \|Δ\| 13.7 %, p90 27 %. The retired ±**5 Å²** was the wrong *shape* — absolute disagreement scales with B itself (r = 0.81), so a fixed Å² band is vacuous below 1.5 Å (where B ≈ 5 Å²) and violated by 8/24 datasets overall. Treat as **weak corroboration**; for a precise value compare like-method or use the deposition's Table 1 | `xtriage` (ML) vs `ctruncate` (classic) | `[benchmark]` (`ref/research/tolerance_benchmark_wilson_b.md`) |
| L-test ⟨\|L\|⟩ | ± **0.02** and **the same twin/no-twin call**. Benchmarked on 27 datasets: median \|Δ\| 0.006, 25/27 inside ±0.02, and the **twin call agreed 27/27** — the call is the load-bearing half, a numeric mismatch with an agreeing call is not a finding. The **"matched resolution range" precondition is unachievable** with default builds (matched in **0 of the 18** datasets where ctruncate reported its range; it restricts the L-test by a median 0.46 Å and exposes no flag to change it), so it is a caveat, not a gate. The full scale is only 0.125 (untwinned 0.500 → perfect twin 0.375), so ±0.02 is ~16 % of range and the observed max (0.047) is 38 %; `xtriage`/`ctruncate` share the Padilla–Yeates *method*, so this checks consistent computation, not method-independence | `xtriage` vs `ctruncate` | `[benchmark]` (`ref/research/tolerance_benchmark_l_test.md`) |
| Completeness (overall) | ± **0.2 pp** vs the validation report's `DataCompleteness` (max observed **0.11 pp** over 9 entries, including one at 62 % completeness — so the tightening is not an artefact of easy cases). The retired ± 1 pp was ~10× too loose. Note the PDBe *experiment* API's `completeness` field is null for every entry tested; use the validation report XML | `phenix.model_vs_data` vs wwPDB validation report | `[benchmark]` (`ref/research/tolerance_benchmark_vs_deposited.md`) |
| Secondary-structure agreement | agent-vs-DSSP three-state ≥ **0.85** over DSSP-assigned residues *(unmeasured — a different comparison)*; two independent assigners: **gate on secondary-structure content, not on agreement**. Three-state agreement is **degenerate at the bad end** — perturbing 1UBQ's coordinates *raises* it (0.75 intact → 0.88 at 1 Å noise → **1.00 at 2 Å**, where both assigners label every residue coil and agree perfectly), so no floor on agreement can work. Require **DSSP H+E content ≥ 0.20** (well-ordered structures 0.46–0.78; perturbed wreckage 0.00–0.11), and given that expect **agreement ≥ 0.65**, fold-class dependent (**α-rich ~0.80–0.85, β-rich ~0.68–0.72**). **High agreement with low SS content is a failure signal, not a pass.** The retired 0.80 floor failed on 12/16 well-ordered structures (ubiquitin, lysozyme, trypsin) while passing the 1 Å and 2 Å wreckage; it had been set from 1SAR (0.865), which scores higher than every entry in the benchmark | agent vs DSSP; DSSP vs biotite P-SEA (`t15_ss_agreement.py`) | `[benchmark]` (`ref/research/tolerance_benchmark_ss_agreement.md`) |
| DockQ score | \|Δ\| ≤ **0.01** (same-implementation noise floor ≈ 0.004; the old ±0.05 was ~12× too loose), and the chain mapping is a **hard gate, not a caveat**: chain-mapping ambiguity is no longer presumed but **proven**, and worth up to **0.79** (~79×) — a plausible mis-mapping took 4HHB and 1BRS from 1.00 to 0.21, i.e. CAPRI *High* → *Incorrect*. **Record the mapping with the score**; a high score does not prove it is right, since symmetric whole-subunit swaps can cost almost nothing (4HHB `CDAB` → 0.98) while partial swaps collapse it. Ambiguity exists only for **homo-oligomers**. The CAPRI-class match is **waived within ±0.03 of a class boundary** (0.23 / 0.49 / 0.80) | agent vs `t16_interface_quality.py` (DockQ) | `[template]` `[benchmark]` (`ref/research/tolerance_benchmark_selection_sensitivity.md`) |
| Interface buried surface area | \|Δ\| ≤ **3 %** of the mean **or 30 Å², whichever is larger**, with matched 1.4 Å probe, protein-only atom selection, and **PISA's per-side `interface_area` doubled** (biotite reports both sides). Measured on 25 interfaces / 17 entries: median \|Δ\| 1.2 %, p90 2.4 %, max 3.7 %. The disagreement is **one-sided** — biotite reads high in 25/25 — so a *negative* Δ is off-distribution and worth investigating. Relative error is size-driven (median 0.9 % above 1200 Å² total vs 2.3 % below), which is why the absolute floor exists. Chain pairs that are **fragments of one molecule** must be excluded before comparing — they are not interfaces | agent vs biotite SASA (`t16_interface_quality.py`); PISA via the PDBe API | `[benchmark]` (`ref/research/tolerance_benchmark_interface_bsa.md`) |
| NMR ensemble precision (mean Cα RMSF) | \|Δ\| ≤ **0.05 Å**, and the ordered-core **cutoff** must match — not merely the intent to use one. Measured over 5 ensembles: varying only the cutoff (1.0–4.0 Å) moves the value by a median **0.14 Å** and up to **0.84 Å** (3–17× the tolerance); reporting the whole-chain mean instead moves it up to **2.15 Å** (43×). **Report the cutoff with the value.** A whole-chain mean must be reported *alongside* an ordered-core figure, not instead of it | agent vs `t17_nmr_ensemble.py` | `[template]` `[benchmark]` (`ref/research/tolerance_benchmark_selection_sensitivity.md`) |
| R-free vs deposited | **State which reference.** \|Δ\| ≤ **0.02** vs the **deposited** `PDB-Rfree` (max observed 0.0128) — that value came from the depositor's own refinement, software and test set. \|Δ\| ≤ **0.01** vs wwPDB's **recomputed** `DCC_Rfree` (median **0.0000**, max 0.0067; 5/9 entries match to four decimals), which is the same derivation `model_vs_data` performs. The two references differ by more than the tolerance | `phenix.model_vs_data` vs wwPDB validation report; REFMAC5 re-refinement | `[catalog]` `[benchmark]` (`ref/research/tolerance_benchmark_vs_deposited.md`) |
| Independent-code-path R offset | \|Δ R\| ≤ **0.02**, with **matched mask radii** (`gemmi sfcalc --radii-set=cctbx`) and the same work set. Benchmarked on 15 entries (1.20–2.92 Å): median **0.0069**, p90 0.0116, max 0.0151, and **one-sided** — gemmi reads high in 15/15, because PHENIX refits k_iso/k_aniso/k_mask **per resolution bin** while gemmi applies one global scale. Using gemmi's default `vdw` mask instead adds a further median +0.0043 (max +0.014), as much again as the offset itself. gemmi is *not* categorically "simpler" — same flat-mask bulk-solvent and anisotropic scaling model | `gemmi sfcalc` vs `phenix.model_vs_data` | `[benchmark]` (`ref/research/tolerance_benchmark_r_offset.md`) |
| H-placement agreement | H-atom count **identical (Δ = 0) for protein-only models**; **void when non-water hetero components are present** — the PHENIX and standalone `reduce` distributions ship **different het dictionaries** (visible in the `USER  MOD` header, e.g. 30TW `std=5902` vs `std=5155`) and the count then diverges by up to **3.96 %**, exceeding even the original ±2 % on 3/18 models. *(The ±0.1 % published in PR #28 was measured on one builder in two conventions, not on the two builders this tolerance names.)* Asn/Gln/His flip sets: **≤ 10 % of flippable residues may differ** between independent H builders — measured **7.5 %** (48 of 639 residues, 17 models, worst model 16.4 %) for `reduce` vs **`mmtbx.reduce2`**. **Exact match is the wrong requirement**: it holds only for `phenix.reduce` vs standalone `reduce`, which are the same binary (`reduce.4.16.250520`, 0 of 634 disagreements). Run reduce2 with `approach=add add_flip_movers=True` — the flip movers default to **off**, in which case it reports nothing and the comparison looks impossible. Clashscore delta within the clashscore tolerance above. **H-count agreement does not imply H-position agreement**: the count is nearly insensitive to the electron-cloud/nuclear convention that dominates the clashscore, so it must not be reported as evidence that two H builds match | standalone `reduce` vs `phenix.reduce` | `[benchmark]` (`ref/research/tolerance_benchmark_clashscore_h.md`) |

> **Method-dependence preconditions (from the domain-expert review — see
> `ref/research/template_tolerance_review.md`).** Two agreement tolerances only hold under a matched
> tool configuration, and comparing without matching will fail for reasons unrelated to a real
> disagreement:
> - **Bond-angle RMSD** depends on the restraint library: PHENIX (CDL) vs gemmi (Engh & Huber / CCP4
>   monomer library) differ by 0.3–0.4° for library reasons alone. Match the restraint library, or use
>   the widened ±0.4° band. **Record the restraint-library and tool versions** with any geometry
>   measurement.
> - **Clashscore** requires a matched hydrogen-build convention (electron-cloud-center for X-ray vs
>   nuclear for neutron/NMR); a mismatch shifts the score (≈ 0.5 on the repo's 1SAR: 3.13 cctbx vs 3.63 standalone — an in-repo observation, not a general benchmark).
>
> **All `[template]` tolerances have now been reviewed** (`ref/research/template_tolerance_review.md`).
> Confirmed defects were fixed: bond-angle (library-conditional), CC½ (citation + 0.1–0.2 floor),
> RSCC (RSZD/RSZO, matched-radius only), aligned-residue count (class-conditional), NMR RMSF
> (matched ordered-core), and the R offset (rationale corrected). Preconditions were added to CA RMSD
> (same selection) and DockQ (fixed chain mapping); Wilson B was loosened to ±5 Å²; L-test, SS
> agreement, bond-length, clashscore, and the Ramachandran/rotamer pp tolerances were kept.
> An independent verified research pass cross-checked all seven of the second batch and confirmed
> these verdicts, additionally tightening **DockQ** to ±0.01 (same-implementation noise floor ≈ 0.004).
>
> **No tolerance is provisional any more.** The two that were — interface BSA and Wilson B — could
> only be settled by running the tools, and now have been (`scripts/bench_t16_bsa_vs_pisa.py`,
> `scripts/bench_t13_wilson_b.py`; audit trails under `ref/research/`). Both changed *shape*, not just
> magnitude: each is now a relative band with an absolute floor, because in both cases the
> disagreement scales with the quantity being measured. **Interface BSA ±10 % → max(3 %, 30 Å²)**
> (~3× tighter on real interfaces; the disagreement is one-sided, biotite high in 25/25).
> **Wilson B ±5 Å² → max(25 %, 2.5 Å²), void under strong anisotropy** — the old band was vacuous
> below 1.5 Å resolution and violated by 8/24 datasets overall.

## 4. Refinement Δ-tolerances (compare→refine flow)

Used by `driving_example.md` when a baseline exists. A refinement must not materially degrade these.

Benchmarked by **re-refining deposited models against their own data** — the null case, where a
correctly-behaving refinement should change almost nothing. The old bands failed that test: 5/8
entries breached the ΔRMSD band and 3/8 the clashscore clause **with no modelling change at all**.
See `ref/research/tolerance_benchmark_refinement_deltas.md`.

> **Detection floor, and what §4 can actually see.** Tested by damaging models with known coordinate
> noise: **degradation below ~0.1 Å Cα RMSD is undetectable by any §4 clause at high resolution**,
> and **below ~0.35 Å at `d_min ≥ 2.5 Å`** — about 3× worse, because null re-refinement alone moves a
> low-resolution model up to 0.285 Å. **§4 is therefore primarily a high-resolution check**: above
> 2.5 Å it catches gross damage only (σ ≳ 0.3 Å), the −6 pp favored clause permits a drop from 92 %
> to 86 %, and the absolute §2 quality bars carry most of the weight
> (`tolerance_benchmark_round6.md`, `tolerance_benchmark_round7.md`) — 7 of 9 models damaged at σ = 0.05 Å (≈ 0.09 Å shift) passed
> everything. That is a property of the quantities, not a tuning gap: 0.09 Å sits inside the
> null-refinement spread. At σ = 0.1 Å (≈ 0.17 Å shift) all 9 were caught, by the ΔRMSD band.

| Check | Tolerance | Provenance |
|---|---|---|
| ΔRMSD sanity | **Resolution-conditional.** `d_min < 2.5 Å`: RMSD_post ≤ RMSD_pre + **0.12 Å** (was 0.10, breached by a null re-refinement on 43SK at **0.1011 Å** once the set reached 19 entries below 2.5 Å). `d_min ≥ 2.5 Å`: + **0.35 Å** (null max 0.285 Å). Validated on **37 entries, 1.37–3.60 Å** (19 below 2.5 Å, of which 11 below 2.0 Å). The band has been widened twice as the set grew: round 8's reported 2× headroom was an artefact of 3 high-resolution entries, round 10 measured 1.15×, and round 11 recorded an outright breach at 0.1011 Å. Re-check whenever entries are added rather than treating it as settled. The underlying dependence is smooth (r = 0.59 with d_min), but a resolution-scaled band is no better for Cα shift and *worse* for favored drop, so the step is kept. A null re-refinement moves a low-resolution model **4× further** than a high-resolution one, so a single band cannot serve both — the earlier +0.05 Å flagged 5/8 correct refinements, and its +0.15 Å replacement (fitted to a 1.37–2.92 Å set) was itself breached by 4/19 once low-resolution entries were included | `[benchmark]` (`ref/research/tolerance_benchmark_refinement_deltas.md`) |
| Geometry did not degrade (**Δ clause**) | **favored is resolution-conditional**: `d_min < 2.5 Å` → favored_post ≥ favored_pre − **0.5 pp** (null max 0.25 pp); `d_min ≥ 2.5 Å` → − **6 pp** (null max **5.26 pp**). The earlier −1.5 pp was breached by 6/19 null re-refinements once low-resolution entries were included. rotamer outliers_post ≤ outliers_pre + **4 pp** (0/19 breaches, worst null rise 3.60 pp). Bands are quoted for **unrestrained** refinement; adding resolution-appropriate NCS + secondary-structure restraints shrinks the low-resolution null spread — max Cα 0.285 → 0.227 Å and max favored drop 5.26 → 3.35 pp, with a **median** change of −0.007 Å (8/11 improved) and **−1.23 pp** (10/11 improved) — so they are conservative by roughly that much when such restraints were used. **Clashscore: gate on the ratio, not the difference, while `clashscore_pre ≲ 20`** — `clashscore_post / clashscore_pre ≥ 5×` is evidence of degradation; every benchmark model separates its null ratio from its 0.1 Å-damage ratio by ≥ **6.7×**, and no null re-refinement in 19 entries reached 5× (max 4.26×, starting clashscores up to **17.2**). **Above pre ≈ 20 the gate fails**: damage drives clashscore to ~100 regardless of starting value, so the ratio collapses (100/pre) — compare the **absolute** post-clashscore against §2's bar there instead. The *difference* stays ungated: on a null re-refinement it moved in both directions (4 up, 4 down, signed median +0.78, range −2.70 to +10.39), so no difference band works. Clashscore is nonetheless the **most sensitive** §4 signal — at 0.05 Å damage it is the only quantity that moves at all | `[benchmark]` (`ref/research/tolerance_benchmark_refinement_deltas.md`) |
| Absolute geometry floors (**split out of the above**) | clashscore ≤ **4**; Ramachandran favored ≥ **97 %**; rotamer outliers ≤ **2 %**. These are **quality bars, not refinement checks** — only 4/8, 4/8 and 6/8 of the *deposited* models in the benchmark meet them. Evaluate and report them separately; never use them to excuse a degradation on an already-poor model | `[literature]` (Chen 2010; Williams 2018 — see §2) |
| Map-model fit did not degrade | **Resolution-conditional.** `d_min < 3.0 Å`: CC_mask_post ≥ CC_mask_pre − **0.04** (14 entries, null min **−0.0311**; was −0.02, breached by 9O9K at 2.90 Å). `d_min ≥ 3.0 Å`: − **0.06** (14 entries, null min −0.0475). **53 EM entries, 1.37–4.11 Å — but only ~15–20 of them are evidence.** Only a *degradation* can breach a `post ≥ pre − x` band; an improvement cannot fail it at any size. Across all entries with published per-entry values, **15 degraded — a lower bound, not a count**: 9 of the 22 in round 12's table, 9O9K in round 13, 0 of 8 in round 14, 4 of 8 in round 15 and 1 of 9 in round 16. Round 13 published only its branch minimum for the 5 other entries it added, so the true figure is **15–20**. Per-entry values from round 14 onward are recorded in `ref/research/data/em_refinement_deltas.tsv`; earlier rounds' are partly unrecoverable. **The degradation *rate* is not set by resolution**: round 15 (3.00–3.90 Å) degraded 4 of 8 while round 16 (3.00–4.11 Å, coarser) degraded 1 of 9. Magnitude is resolution-linked (ρ = +0.397 over 44 entries) but not monotonically — 3.0–3.5 Å carries the largest *median* excursion, 3.5–4.2 Å the largest *single* one. Degradation is **more frequent below 3.0 Å (4/8) than above it**, while being far *smaller* there (≤ 0.0139 versus ≥ 0.0217): frequency and magnitude point at opposite resolutions, and the band width tracks magnitude. Round 14's 8 entries at 2.40–3.10 Å degraded **0** times; round 15's 8 entries at 3.00–3.90 Å degraded **4** times (50 %, worst −0.0351), confirming that the low-resolution regime is where degradations large enough to re-fit a band occur. **Publication clustering was tested and does not predict a similar null Δ** (within-cluster mean |Δ difference| 0.0318 vs 0.0354 between, permutation p = 0.38), so entries from one paper are counted independently. Quote the degradation count, not the entry count: adding entries that improve raises the latter and leaves the former untouched. The 3.0 Å split is conservative — every entry from 2.97 to 3.07 Å is positive and the first excursion past −0.02 is at 3.08 Å — and it is **kept** despite the branches differing by only 1.5×. Round 14 reversed an earlier recommendation to collapse it: a single −0.06 band would indeed hold on every entry measured, but it loosens the `< 3.0 Å` branch by 50 % exactly where the null spread is tighter (worst −0.0311, versus −0.0475 above 3.0 Å), giving 1.93× headroom and blindness to a real 0.05 drop at 2.4 Å. **Headroom against breaching is not the criterion — detection power is**; a band of ±∞ maximises the former. Resolution **bounds** the excursion only loosely, and less well than round 12 stated: that round recorded "every large negative Δ is at ≥ 3.08 Å and all 7 entries below 3.0 Å stay within −0.0139", which 9O9K (2.90 Å, −0.0311) then contradicted. It does **not predict** the excursion either — at ≥ 3.0 Å entries degrade and improve in roughly equal numbers, and no alternative predictor was found (atom count, model-to-map coverage, starting CC_mask). Treat the split as an envelope, not a mechanism. This band has broken three times as the set grew: −0.01 (set on 2 entries) breached at 6; −0.02 breached by 3 of 13; and the `< 3.0 Å` branch's −0.02 (set on 8 entries from one worst case) breached at 14. Each new band is again set just above a single worst case, so treat a further break as the base case. **`d_FSC_model` IS gateable — but not via mtriage's reported value.** `d_FSC_model_post ≤ d_FSC_model_pre + 0.05 Å`, with the crossing computed from `fsc_model.masked.mtriage.log` as the resolution beyond which FSC stays below 0.143 for **20 consecutive shells**. Neither naive rule is safe: the **first** crossing (what mtriage reports) is defeated by one anomalous low-resolution shell, and the **last** crossing by oscillation in the high-resolution tail — on 27WR it returns 2.24 Å for a 2.70 Å map. mtriage reports the **first** shell below the threshold, and a single anomalous low-resolution shell defeats it — 9VJD's curve dips to FSC 0.073 at 23.11 Å and recovers, so mtriage returns 23.11 Å for a 2.86 Å map (last crossing: **2.77 Å**). The bug also hits entries whose 0.143 value looks fine: 21BQ reports 36.11 Å at FSC = 0.5 against a true 2.69 Å. Half-maps do **not** fix it (tested). Supplying both half-maps makes the *half-map* FSC work (27WR: d_fsc 2.61 Å) while leaving the *model-map* crossings degenerate; a coverage explanation was also proposed and withdrawn (21BQ works at 71 atoms per 10⁶ Å³, within 2.6× of the failing 9VJD's 27). The band is retained at ± 0.05 Å: **one-sided**: `d_FSC_model_post ≤ d_FSC_model_pre × 1.05`. Larger `d_FSC_model` is worse, and the clause is "did not degrade", so improvements are unbounded — over **53** entries the largest *degradation* is **+4.79 %** (10BU, 3.20 Å) with **0 breaches** — leaving only **1.045× headroom**, the thinnest margin in this file. Round 16 targeted this band with 9 entries at 3.00–4.11 Å and produced 4 degradations, worst **+1.476 %** (10ME): the band held with 3.4× headroom against that round's worst. Of the **6 degradation magnitudes on record**, the median is 0.240 % and only one exceeds 4 % — so the large excursions are rare rather than routine, and an earlier reading that put 2 of 3 above 4 % was drawing on a **publication-biased sample** in which only the alarming values had been written down. By the same counting argument the band rests on the **14 degradations** among those entries (a firmer count than CC_mask's, since round 13 did publish its `d_FSC_model` direction split), not on 44 observations; round 14 added 8 entries and 0 degradations, round 15 added 8 and 2. Note also that CC_mask and `d_FSC_model` can move in **opposite directions** on the same refinement (10RI: CC_mask +0.0115 while `d_FSC_model` degraded +0.45 %), so the two clauses are not interchangeable evidence, while the largest *change* is a **36 % improvement** (9H7U, 4.06 → 2.59 Å) that a two-sided reading would have failed. The ± 0.05 Å **absolute** band it replaces was genuinely breached, but by 1 entry rather than the 3 first reported — the other two were improvements counted as failures. The quantity ranges **2.2–6.1 Å** across the set, so no absolute band serves both ends — one wide enough to cover the data (0.30 Å) would permit an 11 % change on a 2.7 Å measurement. **Caveat: that range rests on one entry** (9VAM, 6.10 Å; next largest 3.62 Å) — without it an absolute 0.15 Å band would also pass, so the evidence for the *shape* is thinner than for the number. A `|Δd|/d²` band, which the curve's uniform 1/d sampling suggests, was tested and fits *worse* (max/median 15.4 vs 14.0). The three large excursions are genuine curve movement, not estimator jitter: 124, 399 and 117 shells respectively — but note only **one of the three is a degradation** (9VAM); the other two are improvements, so this evidence supports the size of the movement, not a breach count, and that is an **upper bound** — part of each Δ is the sustained estimator's shell quantisation, not model movement (27WR's Δ is ~2 shells) | `[benchmark]` (`ref/research/tolerance_benchmark_refinement_deltas.md`) |

## 5. Calibration checks

Sanity checks against known-good cases; a failure exposes a pipeline bug, not a model defect.

| Check | Expectation | Provenance |
|---|---|---|
| Identity superposition | `1UBQ` onto itself → CA RMSD **0.00 Å**, full-length alignment | `[calibration]` |
| Clean geometry baseline | `3NIR` (0.48 Å) → clashscore ≤ **2**, Ramachandran outliers = **0** | `[calibration]` |
| Resolution calibration | reference-model d_FSC_model within **0.10 Å** of the EMDB-header resolution | `[calibration]` |
| Merged-only data honesty | CC½, ⟨I/σ⟩-outer, R-merge/R-meas reported **unavailable** (not fabricated) when unmerged intensities are absent; `T13_aimless_status` records the abort | `[oracle_tools]` `[catalog]` |

---

## Adding a threshold

1. Add the row here first, with a `[provenance]` tag and — for `[literature]` — a full citation.
2. Cite this file from the driving example that uses it; do not restate the number there.
3. A threshold with no admissible provenance is not admissible (see `CODING_STANDARDS.md` rule 10).
